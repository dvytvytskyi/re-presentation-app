version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: always

  # You can add a db service here if needed later
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: root
  #     POSTGRES_PASSWORD: ${{ secrets.DB_PASSWORD }}
  #     POSTGRES_DB: presentations
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
